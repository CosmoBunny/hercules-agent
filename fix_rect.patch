--- src/tool_panel.rs
+++ src/tool_panel.rs
@@ -895 +895,3 @@
-    let rect = Rect {
+    let fsize = frame.size();
+    
+    let mut rect = Rect {
@@ -900,0 +903,3 @@
+    rect.width = rect.width.min(fsize.width.saturating_sub(rect.x));
+    rect.height = rect.height.min(fsize.height.saturating_sub(rect.y));
+
@@ -910 +915,2 @@
-                Rect {
+                let mut clear_r = Rect {
@@ -915 +921,4 @@
-                },
+                };
+                clear_r.width = clear_r.width.min(fsize.width.saturating_sub(clear_r.x));
+                clear_r.height = clear_r.height.min(fsize.height.saturating_sub(clear_r.y));
+                frame.render_widget(Clear, clear_r);
